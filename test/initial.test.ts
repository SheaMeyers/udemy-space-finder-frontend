

describe('Just check if test setup is working', () => {
    test('works?', () => {
        console.log('Yes it works!')
    })
})

// Prevent warning due to nothing being exported from this file
export {}  
